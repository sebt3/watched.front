{% extends "core.twig" %}
{% block title %}Agents{% endblock %}
{% block contentHeader %}
        Watched
        <small>Agents</small>
{% endblock %}
{% block content %}
          <div class="box box-default">
            <div class="box-header with-border">
              <h3 class="box-title">Available agents</h3>
            </div>
            <div class="box-body table-responsive">
	      <table class="table table-striped table-hover">
	      <thead><tr>
               <th>name</th>
               <th>monitor items</th>
               <th>current events</th>
               <th>ressources</th>
	      </tr></thead><tbody>
    {% for a in agents %}
               <tr>
                <td><a href="{{ path_for('agent', { 'id': a.id }) }}">{{ a.host }}</a></td>
                <td>{{ a.monitor_items }}</td>
                <td>{{ a.events }}</td>
                <td><a href="{{ path_for('ressources', { 'id': a.id }) }}">{{ a.ressources }}</a></td>
               </tr>
    {% endfor %}
	      </tbody>
	     </table>
            </div>
          </div>
{% endblock %}
