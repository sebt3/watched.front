{% extends "core.twig" %}
{% block title %}{{ _('Teams') }} - {{ _('Add') }}{% endblock %}
{% block contentHeader %}
        {{ _('Teams') }}
        <small>{{ _('create a team') }}</small>
{% endblock %}
{% block scripts %}
<script>
wd.dispatch.on('ready', function() {
d3.select('section.content').call(bs.row()
	.cell('col-lg-8 col-lg-offset-2', bs.form().url("{{ path_for('admin.teams.add') }}").body(bs.box().class('box-info')
		.title("{{ _('Create a team') }}").body(bs.union()
			.item(bs.formGroup('name').label("{{ _('Name') }}").value("{{ name }}", "{{ _('Name') }}"))
		).footer(bs.button.group()
			.left(bs.button.a().text("{{ _('Cancel') }}").icon("fa fa-arrow-left").url("{{ path_for('admin.teams.list') }}"))
			.right(bs.button.form().text("{{ _('Add') }}").icon("fa fa-plus").class('btn-info'))
))));
});
</script>
{% endblock %}
