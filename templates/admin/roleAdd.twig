{% extends "core.twig" %}
{% block title %}{{ _('Roles') }} - {{ _('Add') }}{% endblock %}
{% block contentHeader %}
        {{ _('Roles') }}
        <small>{{ _('create a role') }}</small>
{% endblock %}
{% block scripts %}
<script>
d3.select('section.content').call(bs.row()
	.cell('col-lg-8 col-lg-offset-2', bs.form().url("{{ path_for('admin.roles.add') }}").body(bs.box().class('box-info')
		.title("{{ _('Create a role') }}").body(bs.union()
			.item(bs.formGroup('name').label("{{ _('Name') }}").value("{{ name }}", "{{ _('Name') }}"))
		).footer(bs.button.group()
			.left(bs.button.a().text("{{ _('Cancel') }}").icon("fa fa-arrow-left").url("{{ path_for('admin.roles.list') }}"))
			.right(bs.button.form().text("{{ _('Add') }}").icon("fa fa-plus").class('btn-info'))
))));
</script>
{% endblock %}
